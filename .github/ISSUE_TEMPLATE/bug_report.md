---
name: Bug report
about: Something in the plugin misbehaves (hook, statusline, stats, evals)
labels: bug
---

**What happened**

**Expected**

**Environment**
- OS:
- Claude Code version (`claude --version`):
- Model in use:
- `python3 --version`:

**Repro**
Steps or the exact command. For statusline issues, include the output of:
`echo '{}' | ~/.claude/fable-mode/statusline.sh`
